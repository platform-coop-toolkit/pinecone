{% if not standAlone %}<div class="spacer"></div>{% endif %}
<div class="input-group">
	<input id="{{ name }}" name="{{ name }}" placeholder="{{ placeholder }}" aria-label="{{ label }}" type="search"{% if disabled %} disabled{% endif %} />
</div>
