{% if not standAlone %}<div class="spacer"></div>{% endif %}
<button class="filter-button" aria-expanded="false"><span class="screen-reader-text">{{ label }}</span>{% render '@svg', {svg: 'add'}, true %}</button>
